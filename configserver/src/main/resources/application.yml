#
#
#
server:
  port: 8888
  application:
    name: config-server
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/